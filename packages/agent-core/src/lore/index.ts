/**
 * The Lore public surface: the brain (the store) and ingestion (the adapters
 * that feed it). Imported as `agent-core/lore`. Node-only — it touches `fs`.
 */
export * from "./brain";
export * from "./ingest";
